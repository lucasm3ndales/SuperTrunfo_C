void shuffle(cheap card[24]) {
    srand(time(NULL));
    int aux, ex;

    for(int i = 0; i < 24; i++) {
       
    }

    for(int i = 0; i < 24; i++) {
        printf("%s, %c, %d, %d, %d, %d\n", card[i].name, card[i].type, card[i].st,card[i].intel,card[i].alco,card[i].nihil);
    }   
  
}